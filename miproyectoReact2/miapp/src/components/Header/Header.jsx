const Header = (props) =>{
    //props recibe parametros y esta tipo objeto

    return(

        <header className="Header">
            <h5>{props.esuntexto}</h5>
        </header>

    )
}

export {Header}